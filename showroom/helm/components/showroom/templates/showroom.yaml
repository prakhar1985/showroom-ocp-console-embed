{{- if .Values.showroom.enabled }}
---
# Showroom Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.showroom.namespace | default "showroom" }}
  labels:
    demo.redhat.com/application: "showroom"
---
# Showroom ServiceAccount
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.showroom.name }}
  namespace: {{ .Values.showroom.namespace }}
---
# Showroom RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: edit-{{ .Values.showroom.name }}-sa
  namespace: {{ .Values.showroom.namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: edit
subjects:
- kind: ServiceAccount
  name: {{ .Values.showroom.name }}
  namespace: {{ .Values.showroom.namespace }}
---
# Showroom Panel Toggle Script
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.showroom.name }}-toggle-script
  namespace: {{ .Values.showroom.namespace }}
data:
  showroom-toggle.js: |
    (function () {
      'use strict';
      /*
       * Showroom panel mode switcher.
       *
       * Toolbar at the top-right lets users switch between:
       *   "instructions" – lab guide full-width (read the step)
       *   "split"        – both panes side by side
       *   "tabs"         – tabs/console full-width (do the task)
       *
       * View is stored in URL hash (#view=...) for sharing/bookmarking.
       * Split widths are saved so resize → tabs → split restores correctly.
       *
       * Keyboard: Ctrl+1 = instructions, Ctrl+2 = split, Ctrl+3 = tabs
       * (only works when parent page has focus; cross-origin iframes
       *  block key events — hover toolbar to refocus parent)
       */
      var HASH_KEY = 'view';
      var STORE_KEY = 'sr-panel-mode';
      var DEFAULT_MODE = '{{ .Values.showroom.panelMode | default "instructions" }}';
      var MAX_RETRIES = 30;
      var retries = 0;

      /* ── URL hash helpers ──────────────────────────────── */
      function getHashParam() {
        var m = location.hash.match(/[#&]view=(instructions|split|tabs)/);
        return m ? m[1] : null;
      }
      function setHashParam(v) {
        history.replaceState(null, '', '#' + HASH_KEY + '=' + v);
      }

      function init() {
        var gutter = document.querySelector('.gutter.gutter-horizontal')
                  || document.querySelector('.gutter')
                  || document.querySelector('[class*="Resizer"]');
        if (!gutter) {
          if (++retries < MAX_RETRIES) { setTimeout(init, 400); }
          return;
        }

        var left  = gutter.previousElementSibling;
        var right = gutter.nextElementSibling;
        if (!left || !right) return;

        /* ── styles ───────────────────────────────────────── */
        var css = document.createElement('style');
        css.textContent = [
          /* Toolbar — top-right, inline with header */
          '.sr-toolbar{',
          '  position:fixed;top:8px;right:12px;',
          '  z-index:99999;display:flex;align-items:center;gap:0;',
          '  background:#1a1a1a;border-radius:8px;',
          '  box-shadow:0 2px 12px rgba(0,0,0,.25),0 0 0 1px rgba(255,255,255,.08);',
          '  padding:3px;',
          '  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;',
          '}',
          /* Mode buttons */
          '.sr-mode-btn{',
          '  display:flex;align-items:center;gap:6px;',
          '  padding:7px 14px;border:none;border-radius:6px;',
          '  cursor:pointer;font-size:13px;font-weight:500;',
          '  color:#999;background:transparent;',
          '  transition:all .2s ease;white-space:nowrap;',
          '  line-height:1;',
          '}',
          '.sr-mode-btn:hover{color:#fff;background:rgba(255,255,255,.1)}',
          '.sr-mode-btn.sr-active{',
          '  color:#fff;background:#ee0000;',
          '  box-shadow:0 2px 8px rgba(238,0,0,.35);',
          '}',
          '.sr-mode-btn svg{width:15px;height:15px;fill:currentColor;flex-shrink:0}',
          /* Separator */
          '.sr-sep{width:1px;height:18px;background:rgba(255,255,255,.12);flex-shrink:0}',
          /* First-visit hint */
          '.sr-hint{',
          '  position:fixed;top:48px;right:12px;',
          '  z-index:99999;background:#1a1a1a;color:#ccc;',
          '  font-size:12px;padding:8px 14px;border-radius:8px;',
          '  box-shadow:0 4px 16px rgba(0,0,0,.3);',
          '  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;',
          '  white-space:nowrap;opacity:1;transition:opacity .5s;pointer-events:none;',
          '}',
          '.sr-hint::before{',
          '  content:"";position:absolute;bottom:100%;right:24px;',
          '  border:6px solid transparent;border-bottom-color:#1a1a1a;',
          '}',
          /* Mode overrides — CSS !important beats Split.js inline styles */
          'body.sr-mode-instructions .sr-right,',
          'body.sr-mode-instructions .sr-gutter{display:none!important}',
          'body.sr-mode-instructions .sr-left{width:100%!important;max-width:100%!important}',
          'body.sr-mode-tabs .sr-left,',
          'body.sr-mode-tabs .sr-gutter{display:none!important}',
          'body.sr-mode-tabs .sr-right{width:100%!important;max-width:100%!important}',
        ].join('\n');
        document.head.appendChild(css);

        /* ── SVG icons ────────────────────────────────────── */
        var icoDoc = '<svg viewBox="0 0 24 24"><path d="M14 2H6c-1.1 0-2 .9-2 2v16c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V8l-6-6zM6 20V4h7v5h5v11H6z"/></svg>';
        var icoSplit = '<svg viewBox="0 0 24 24"><path d="M3 3h8v18H3V3zm10 0h8v18h-8V3zM5 5v14h4V5H5zm10 0v14h4V5h-4z"/></svg>';
        var icoTabs = '<svg viewBox="0 0 24 24"><path d="M21 3H3c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h18c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H3V5h10v4h8v10zM15 5h2v2h-2V5zm4 0h2v2h-2V5z"/></svg>';

        /* ── build toolbar ────────────────────────────────── */
        var toolbar = document.createElement('div');
        toolbar.className = 'sr-toolbar';

        var btnI = document.createElement('button');
        btnI.className = 'sr-mode-btn';
        btnI.innerHTML = icoDoc + '<span>Instructions</span>';
        btnI.title = 'Full-width instructions (Ctrl+1)';
        btnI.setAttribute('data-mode', 'instructions');

        var sep1 = document.createElement('div');
        sep1.className = 'sr-sep';

        var btnS = document.createElement('button');
        btnS.className = 'sr-mode-btn';
        btnS.innerHTML = icoSplit + '<span>Split</span>';
        btnS.title = 'Side by side (Ctrl+2)';
        btnS.setAttribute('data-mode', 'split');

        var sep2 = document.createElement('div');
        sep2.className = 'sr-sep';

        var btnC = document.createElement('button');
        btnC.className = 'sr-mode-btn';
        btnC.innerHTML = icoTabs + '<span>Tabs</span>';
        btnC.title = 'Full-width tabs (Ctrl+3)';
        btnC.setAttribute('data-mode', 'tabs');

        toolbar.appendChild(btnI);
        toolbar.appendChild(sep1);
        toolbar.appendChild(btnS);
        toolbar.appendChild(sep2);
        toolbar.appendChild(btnC);
        document.body.appendChild(toolbar);

        /* Hovering toolbar refocuses parent so keyboard shortcuts work
           even after clicking inside a cross-origin iframe */
        toolbar.addEventListener('mouseenter', function () { window.focus(); });

        var mode = 'split';

        /* ── Tag panes so CSS can target them ────────────── */
        left.classList.add('sr-left');
        right.classList.add('sr-right');
        gutter.classList.add('sr-gutter');

        /* ── panel manipulation ───────────────────────────── */
        /* Uses CSS classes on <body> instead of touching inline styles.
           Split.js's inline widths are NEVER modified, so switching
           back to split always restores the correct widths. */
        function apply() {
          btnI.classList.toggle('sr-active', mode === 'instructions');
          btnS.classList.toggle('sr-active', mode === 'split');
          btnC.classList.toggle('sr-active', mode === 'tabs');

          document.body.classList.remove('sr-mode-instructions', 'sr-mode-tabs');
          if (mode === 'instructions') {
            document.body.classList.add('sr-mode-instructions');
          } else if (mode === 'tabs') {
            document.body.classList.add('sr-mode-tabs');
          } else {
            /* split — just let Split.js's inline styles take over */
            setTimeout(function () {
              window.dispatchEvent(new Event('resize'));
            }, 50);
          }
          setHashParam(mode);
          try { localStorage.setItem(STORE_KEY, mode); } catch (e) {}
        }

        function setMode(m) { mode = m; apply(); }

        /* ── button clicks ────────────────────────────────── */
        toolbar.addEventListener('click', function (e) {
          var btn = e.target.closest('.sr-mode-btn');
          if (!btn) return;
          e.preventDefault();
          setMode(btn.getAttribute('data-mode'));
        });

        /* ── keyboard shortcuts ───────────────────────────── */
        document.addEventListener('keydown', function (e) {
          if (!e.ctrlKey) return;
          if (e.key === '1') { e.preventDefault(); setMode('instructions'); }
          if (e.key === '2') { e.preventDefault(); setMode('split'); }
          if (e.key === '3') { e.preventDefault(); setMode('tabs'); }
        });

        /* ── handle hash changes (back/forward navigation) ── */
        window.addEventListener('hashchange', function () {
          var h = getHashParam();
          if (h && h !== mode) setMode(h);
        });

        /* ── first-visit hint ─────────────────────────────── */
        var hintKey = 'sr-hint-dismissed';
        try {
          if (!localStorage.getItem(hintKey)) {
            var hint = document.createElement('div');
            hint.className = 'sr-hint';
            hint.textContent = 'Switch views with the toolbar above \u2022 Ctrl+1 / Ctrl+2 / Ctrl+3';
            document.body.appendChild(hint);
            localStorage.setItem(hintKey, '1');
            setTimeout(function () {
              hint.style.opacity = '0';
              setTimeout(function () { hint.remove(); }, 600);
            }, 8000);
          }
        } catch (e) {}

        /* ── restore mode: URL hash > localStorage > default ── */
        try {
          var fromHash = getHashParam();
          var fromStore = localStorage.getItem(STORE_KEY);
          if (fromHash) {
            mode = fromHash;
          } else if (fromStore === 'tabs' || fromStore === 'instructions' || fromStore === 'split') {
            mode = fromStore;
          } else {
            mode = DEFAULT_MODE;
          }
        } catch (e) { mode = DEFAULT_MODE; }
        apply();
      }

      if (document.readyState === 'complete') setTimeout(init, 600);
      else window.addEventListener('load', function () { setTimeout(init, 600); });
    })();
---
# Showroom Nginx ConfigMap
kind: ConfigMap
metadata:
  name: {{ .Values.showroom.name }}-proxy-config
  namespace: {{ .Values.showroom.namespace }}
apiVersion: v1
data:
  nginx.conf: |
    events {
    }
    error_log /dev/stdout info;
    http {
      include /etc/nginx/mime.types;
      proxy_cache off;
      expires -1;
      proxy_cache_path /dev/null keys_zone=mycache:10m;
      map $http_upgrade $connection_upgrade {
          default upgrade;
          '' close;
      }
      server {
        listen 8080;
        absolute_redirect off;
        # Serve toggle script from ConfigMap
        location = /showroom-toggle.js {
          alias /etc/nginx/toggle/showroom-toggle.js;
          default_type application/javascript;
        }
        location / {
          proxy_pass http://127.0.0.1:8000;
          expires off;
          proxy_cache off;
          proxy_pass_request_headers on;
          # Empty Accept-Encoding so upstream sends uncompressed HTML
          # (required for sub_filter to work)
          proxy_set_header Accept-Encoding "";
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
          # Inject toggle script into HTML pages
          sub_filter '</head>' '<script src="/showroom-toggle.js" defer></script></head>';
          sub_filter_once on;
          sub_filter_types text/html;
        }
        {{- if and .Values.showroom.terminal.enabled (eq .Values.showroom.terminal.type "ocp") }}
        location /terminal/ {
          proxy_pass http://127.0.0.1:7681;
          rewrite ^/terminal/(.*)$ /$1 break;
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection "upgrade";
          proxy_read_timeout 43200000;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header Host $http_host;
          proxy_set_header X-NginX-Proxy true;
        }
        {{- end }}
        {{- if and .Values.showroom.terminal.enabled (ne .Values.showroom.terminal.type "ocp") }}
        location ^~ /wetty {
          proxy_pass http://127.0.0.1:7681/wetty;
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection "upgrade";
          proxy_read_timeout 43200000;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header Host $http_host;
          proxy_set_header X-NginX-Proxy true;
        }
        {{- end }}
      }
    }
---
# Showroom User Data ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.showroom.name }}-userdata
  namespace: {{ .Values.showroom.namespace }}
data:
  user_data.yml: |
    cluster_domain: {{ .Values.deployer.domain }}
    demo_namespace: {{ .Values.demo.namespace }}
    demo_app: {{ .Values.demo.appName }}
    {{- range $key, $value := .Values.showroom.userdata }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
{{- if .Values.showroom.tabs }}
---
# Showroom UI Config (overrides content repo ui-config.yml)
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.showroom.name }}-ui-config
  namespace: {{ .Values.showroom.namespace }}
data:
  ui-config.yml: |
    ---
    type: showroom
    default_width: 30
    persist_url_state: true
    tabs:
    {{- range .Values.showroom.tabs }}
      - name: {{ .name | quote }}
        url: {{ .url | quote }}
    {{- end }}
{{- end }}
{{- if .Values.showroom.terminal.enabled }}
---
# Showroom Terminal PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.showroom.name }}-terminal-lab-user-home
  namespace: {{ .Values.showroom.namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: "5Gi"
{{- end }}
---
# Showroom Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.showroom.name }}
  namespace: {{ .Values.showroom.namespace }}
spec:
  type: ClusterIP
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: {{ .Values.showroom.name }}
---
# Showroom Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.showroom.name }}
  namespace: {{ .Values.showroom.namespace }}
  labels:
    demo.redhat.com/application: "showroom"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.showroom.name }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.showroom.name }}
    spec:
      serviceAccountName: {{ .Values.showroom.name }}
      initContainers:
      - name: git-cloner
        image: "quay.io/rhpds/git-cloner:v1.1.2"
        env:
        - name: GIT_REPO_URL
          value: {{ .Values.showroom.content.repoUrl | quote }}
        - name: GIT_REPO_REF
          value: {{ .Values.showroom.content.repoRef | quote }}
        - name: CLONE_DIR
          value: "/showroom/repo"
        volumeMounts:
        - mountPath: /showroom/repo
          name: showroom-files
          subPath: repo
      - name: antora-builder
        image: "quay.io/rhpds/antora:v1.0.1"
        env:
        - name: FILES_DIR
          value: "/showroom/repo"
        - name: OUTPUT_DIR
          value: "/showroom/www"
        - name: ANTORA_SITE_FILE
          value: {{ .Values.showroom.content.antoraPlaybook | quote }}
        volumeMounts:
        - mountPath: /showroom/repo
          name: showroom-files
          subPath: repo
        - name: user-data
          mountPath: /user_data/
        - mountPath: /showroom/www
          name: showroom-files
          subPath: www
      containers:
      - name: nginx
        image: "quay.io/rhpds/nginx:1.25"
        imagePullPolicy: "IfNotPresent"
        volumeMounts:
        - mountPath: /etc/nginx/nginx.conf
          name: nginx-config
          subPath: nginx.conf
        - mountPath: /etc/nginx/toggle
          name: toggle-script
        - mountPath: /var/cache/nginx
          name: nginx-cache
        - mountPath: /var/run
          name: nginx-pid
        ports:
        - name: web
          containerPort: 8080
      - name: content
        image: {{ .Values.showroom.content.image | quote }}
        imagePullPolicy: "IfNotPresent"
        env:
        - name: GIT_REPO_URL
          value: {{ .Values.showroom.content.repoUrl | quote }}
        - name: GIT_REPO_REF
          value: {{ .Values.showroom.content.repoRef | quote }}
        - name: ANTORA_PLAYBOOK
          value: {{ .Values.showroom.content.antoraPlaybook | quote }}
        - name: DOMAIN
          value: {{ .Values.deployer.domain | quote }}
        {{- if .Values.showroom.nookbag.enabled }}
        - name: ZT_BUNDLE
          value: {{ .Values.showroom.nookbag.bundleUrl | quote }}
        - name: ZT_UI_ENABLED
          value: "true"
        - name: GUID
          value: {{ .Values.showroom.namespace | quote }}
        {{- end }}
        volumeMounts:
        - name: user-data
          mountPath: /user_data/
        - mountPath: /showroom/repo
          name: showroom-files
          subPath: repo
        - mountPath: /showroom/www
          name: showroom-files
          subPath: www
        {{- if .Values.showroom.tabs }}
        - name: ui-config
          mountPath: /showroom/repo/ui-config.yml
          subPath: ui-config.yml
        {{- end }}
        ports:
        - containerPort: 8000
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: 8000
        readinessProbe:
          httpGet:
            path: /
            port: 8000
      {{- if and .Values.showroom.terminal.enabled (eq .Values.showroom.terminal.type "ocp") }}
      - name: terminal
        image: {{ .Values.showroom.terminal.image | quote }}
        imagePullPolicy: IfNotPresent
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        volumeMounts:
        - name: terminal-lab-user-home
          mountPath: /home/lab-user
        ports:
        - containerPort: 7681
          protocol: TCP
        resources:
          limits:
            cpu: {{ .Values.showroom.terminal.resources.limits.cpu }}
            memory: {{ .Values.showroom.terminal.resources.limits.memory }}
          requests:
            cpu: {{ .Values.showroom.terminal.resources.requests.cpu }}
            memory: {{ .Values.showroom.terminal.resources.requests.memory }}
      {{- end }}
      {{- if and .Values.showroom.terminal.enabled (ne .Values.showroom.terminal.type "ocp") }}
      - name: wetty
        image: {{ .Values.showroom.terminal.wettyImage | default "quay.io/rhpds/wetty:v2.5" | quote }}
        imagePullPolicy: IfNotPresent
        args:
        - --base=/wetty/
        - --port=7681
        {{- if .Values.showroom.terminal.bastion.host }}
        - --ssh-host={{ .Values.showroom.terminal.bastion.host }}
        - --ssh-port={{ .Values.showroom.terminal.bastion.port | default "22" }}
        - --ssh-user={{ .Values.showroom.terminal.bastion.user }}
        - --ssh-auth=password
        - --ssh-pass={{ .Values.showroom.terminal.bastion.password }}
        {{- end }}
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        ports:
        - containerPort: 7681
          protocol: TCP
        resources:
          limits:
            cpu: {{ .Values.showroom.terminal.resources.limits.cpu }}
            memory: {{ .Values.showroom.terminal.resources.limits.memory }}
          requests:
            cpu: {{ .Values.showroom.terminal.resources.requests.cpu }}
            memory: {{ .Values.showroom.terminal.resources.requests.memory }}
      {{- end }}
      volumes:
      - name: showroom-files
        emptyDir: {}
      - name: user-data
        configMap:
          name: {{ .Values.showroom.name }}-userdata
      - name: nginx-config
        configMap:
          defaultMode: 420
          name: {{ .Values.showroom.name }}-proxy-config
      - name: nginx-pid
        emptyDir: {}
      - name: nginx-cache
        emptyDir: {}
      - name: toggle-script
        configMap:
          name: {{ .Values.showroom.name }}-toggle-script
      {{- if .Values.showroom.terminal.enabled }}
      - name: terminal-lab-user-home
        persistentVolumeClaim:
          claimName: {{ .Values.showroom.name }}-terminal-lab-user-home
      {{- end }}
      {{- if .Values.showroom.tabs }}
      - name: ui-config
        configMap:
          name: {{ .Values.showroom.name }}-ui-config
      {{- end }}
---
# Showroom Route
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.showroom.name }}
  namespace: {{ .Values.showroom.namespace }}
  annotations:
    haproxy.router.openshift.io/timeout: 1h
    haproxy.router.openshift.io/timeout-tunnel: 1h
spec:
  to:
    kind: Service
    name: {{ .Values.showroom.name }}
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
{{- end }}
